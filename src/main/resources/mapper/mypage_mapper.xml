<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eat.mypage.MypageDAO">

	<!-- 회원정보 수정 -->
	<update id="member_update"
		parameterType="com.eat.mypage.MypageDTO">
		UPDATE member SET email = #{email},
		nickname =
		#{nickname},
		bio = #{bio},
		location = #{location},
		withdraw = #{withdraw}
		WHERE user_id = #{user_id}
	</update>

	<!--회원 정보 수정 - 닉네임 중복체크 -->
	<select id="nickNameOverlay" resultType="int"
		parameterType="map">
		SELECT COUNT(*) FROM member
		WHERE nickname = #{nickname}
		AND user_id != #{user_id}
	</select>

	<!-- 회원 정보 수정 - 이메일 중복체크 -->
	<select id="emailOverlay" resultType="int" parameterType="map">
		SELECT
		COUNT(*) FROM member
		WHERE email = #{email} AND user_id != #{user_id}
	</select>

	<!-- 회원 정보 수정 - 태그 삭제 -->
	<delete id="deletMemberTags" parameterType="String">
		delete from
		tag_prefer where user_id = #{user_id}
	</delete>

	<!-- 회원 정보 수정 - 태그 추가 -->
	<insert id="addMemberTags" parameterType="map">
		INSERT INTO tag_prefer (user_id, idx, class)
		VALUES
		<foreach collection="tagList" item="tag" separator=",">
			(#{user_id},#{tag.idx},#{tag.isClass})
		</foreach>
	</insert>

	<!-- 회원 정보 수정 - 기존 이미지 파일 덮어쓰기 -->
	<update id="fileupdate">
		UPDATE photo
		SET ori_filename = #{ori_filename},
		new_filename = #{filename}
		WHERE img_idx = #{img_idx}
	</update>

	<!-- 회원 정보 수정 - 새 이미지 추가 -->
	<insert id="fileInsert" useGeneratedKeys="true"
		keyProperty="img_idx">
		INSERT INTO photo (ori_filename, new_filename)
		VALUES (#{ori_filename} #{new_filename})
	</insert>

	<!-- 회원 정보 수정 - 넣은 이미지의 img_idx 가져오기 -->
	<select id="getinsertImgidx" resultType="int">
		SELECT LAST_INSERT_ID()
	</select>

	<!-- 회원 정보 수정 -회원 테이블에 새 img_idx 저장 -->
	<update id="updateMemberImgIdx">
		UPDATE member
		SET img_idx = #{newImg_Idx}
		WHERE user_id = #{user_id}
	</update>

	<!-- 이미지 정보 가져오기 -->
	<select id="imgInfo" resultType="map">
		SELECT s.ori_filename, s.new_filename
		FROM photo p
		JOIN sts s on p.img_idx = s.img_idx
		WHERE p.img_idx = #{img_idx}
	</select>



</mapper>