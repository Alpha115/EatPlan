<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eat.main.MypageDAO">
	
	<!-- 회원정보 수정 -->
	<update id="member_update" parameterType="com.eat.main.MypageDTO">
	UPDATE member SET email = #{email},
        nickname = #{nickname},
        bio = #{bio},
        location = #{location},
        withdraw = #{withdraw}
    WHERE user_id = #{user_id}
	</update>
	
</mapper>